pipeline {
		agent any 
			stages {
				stage('cloning') {
					steps {
						sh 'git clone'
					}
					
				}
				stage ('testcases') {
					steps {
						sh 'execute testcases'
					}
				}
				stage ('building') {
					steps {
						sh 'building'
					}
				}
			}
		
	}